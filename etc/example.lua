-- This is an example file with stuff.

local function testingFunction(param1, param2)
    return param1 + param2
end

local arg1 = 25
local arg2 = 300

local total = testingFunction(arg1, arg2)

print(tonumber(total))
